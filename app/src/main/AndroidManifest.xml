<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kansus.ksnes"
    android:installLocation="preferExternal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Para Android 10 (API 29) e superior, esta permissão tem efeito limitado. -->
    <!-- Considere usar o Scoped Storage se estiver atualizando o app. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <!-- Para Android 9 (API 28) e superior, é necessária para serviços em primeiro plano. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".KSNESApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@drawable/app_icon"
        android:label="@string/app_label"
        android:requestLegacyExternalStorage="true">
        <!-- android:requestLegacyExternalStorage="true" é importante para o funcionamento das permissões de armazenamento em Android 10+ -->

        <activity
            android:name=".activity.MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name="CreateShortcuts"
            android:label="@string/launcher_shortcut_name"
            android:exported="true"
            android:targetActivity=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity-alias>

        <activity android:name=".activity.FileChooser" />
        <activity
            android:name=".activity.StateSlotsActivity"
            android:theme="@android:style/Theme.Dialog" />
        <activity android:name=".activity.KeyProfilesActivity" />
        <activity android:name=".activity.HelpActivity" />
        <activity
            android:name=".activity.CheatsActivity"
            android:process=":emulator" />
        <activity
            android:name=".activity.EmulatorSettings"
            android:process=":emulator" />

        <activity
            android:name=".activity.EmulatorActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:launchMode="singleTask"
            android:process=":emulator"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="file" />
                <data android:mimeType="application/zip" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.EmulatorService"
            android:process=":emulator" />

    </application>

</manifest>
